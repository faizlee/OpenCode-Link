# OpenCodex Link 文档与证据入口

> **文档处置**：`current-source`。这是产品决定、功能传播记录与交付证据的唯一导航；产品使用方式仍以仓库根目录 `README.md` 为准。

## 事实源顺序

1. `docs/product-decisions.json`：用户已确认的产品决定及明确延期项。
2. `README.md`：当前产品范围、运行方式、安全边界和验证边界。
3. `docs/desktop-console-design.md`：已确认的托盘、电脑管理台与 Redline 风格设计；源码为实现候选，真实验收仍待主控。
4. 当前 Git 提交与源码：真实实现状态。
5. `docs/lifecycle/*.decision.json`：每项决定同步到哪些当前消费者，以及下一步允许做什么。
6. `docs/lifecycle/*.receipt.json`：由校验器从对应 decision 文件生成的结构化回执，不代替真实手机验收或 Git 提交。

## 当前交付基线

- 实现提交：`3c97bac`（完整移动端交付）与 `0ffa34c`（重复扫码及固定名称迁移复用设备身份）。
- 自动验证：`npm run typecheck`、`npm test`、`npm run build` 通过；当前为 17 个测试文件、56 项测试，覆盖单一主二维码、可靠初始入口、可撤销路径凭据、每个来源独立会话、恶意候选过滤、局域网与 Tailscale 双向切换、最新任务日志、设备命名和身份复用。
- 本机宿主验证：设置页、任务列表、历史同步、移动端布局、桌面入口说明、可信设备重启恢复与解除、重复扫码不新增设备、固定名称迁移不新增设备、固定名称 80→8787 转发通过。
- 历史正文保护：手机页面不再显示启动时注入的插件清单、AGENTS 指令和环境上下文，只显示真实用户消息。
- 启动配置：重复运行启动脚本后，`HOST`、`PORT`、`LAN_NAME` 三项受管配置各保留一份；旧版 `CODEX_PWA_PASSWORD` 会被移除，扫码设备凭证继续保存在用户数据目录。

## 已确认的下一阶段方向

- `DESKTOP-TRAY-WEB-CONSOLE-001` 仍保持 `contract_ready`：托盘 Owner、五区 Redline 管理台、单一配对二维码、内部连接选择和便携包已有实现候选；本机真实浏览器已模拟“仅局域网配对 → 局域网失联/Tailscale 可用 → Tailscale 失联/局域网恢复”，三段均直接进入任务列表、未出现扫码页且设备记录保持一条，仍不等于真实手机与目标机验收完成。
- 管理台使用 faizleecom `time-ai-arms-race-2023@0.1.0` 的 Redline 视觉合同做界面化适配，不把文章组件或第三方品牌资产直接复制进软件。
- 用户只扫码一次绑定手机与电脑；局域网、固定名称、Tailscale 及后续其他连接方式由软件内部自动选择和无感切换，管理台主路径不再提供多二维码或网络选项。技术状态仍可在连接详情中查看，Tailscale 不参加物理局域网 mDNS 广播。
- 下一步是完成真实手机网络切换、NotifyIcon、未知占用和目标机便携包验收；在此之前不把状态提升为 `delivery_integrated`，也不做 force、PR 或 release。

## 仍需真实手机复验

- 目标手机与当前 Wi-Fi 对 `.local` 的实际解析；失败时应继续停留在扫码得到的 IP 入口。
- 目标手机网络条件变化后，软件能在局域网、固定名称和 Tailscale 之间自动选择可达路径，保持同一设备身份且设备数不增加。
- 浏览器要求用户确认后的桌面图标安装与再次进入。
- 手机选择多附件、原生图片队列失败时的本地路径回退，以及电脑任务中的实际可读结果。

上述事项完成前，不把本轮状态提升为 `delivery_integrated`，也不宣称已经发布或具备公网访问能力。
