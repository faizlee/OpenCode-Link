# OpenCodex Link 文档与证据入口

> **文档处置**：`current-source`。这是产品决定、功能传播记录与交付证据的唯一导航；产品使用方式仍以仓库根目录 `README.md` 为准。

## 事实源顺序

1. `docs/product-decisions.json`：用户已确认的产品决定及明确延期项。
2. `README.md`：当前产品范围、运行方式、安全边界和验证边界。
3. `docs/desktop-console-design.md`：已确认的托盘、电脑管理台与 Redline 风格设计；源码为实现候选，真实验收仍待主控。
4. 当前 Git 提交与源码：真实实现状态。
5. `docs/lifecycle/*.decision.json`、`*.contract.json` 与 `*.delivery.json`：决定、合同和交付阶段同步到哪些当前消费者，以及下一步允许做什么。
6. `docs/lifecycle/*.receipt.json`：由校验器从对应生命周期文件生成的结构化回执，不代替真实手机验收或 Git 提交。

## 当前交付基线

- 实现提交：`3c97bac`（完整移动端交付）与 `0ffa34c`（重复扫码及固定名称迁移复用设备身份）。
- 自动验证：`npm run typecheck`、`npm test`、`npm run build` 通过；当前为19个测试文件、62项测试。新增证据覆盖任务文件引用、规范路径与允许根目录、认证、短时令牌、主动内容阻断、下载和删除后410；旧“跨来源自动接力”证据仍不等于新“双快捷入口”决定已经实施。
- 本机宿主验证：设置页、任务列表、历史同步、移动端布局、桌面入口说明、可信设备重启恢复与解除、任务内图片/文本/不支持格式预览、重复扫码不新增设备、固定名称迁移不新增设备、固定名称 80→8787 转发通过。
- `THREAD-FILE-PREVIEW-001` 已集成到 `codex/desktop-tray-redline` 的 `1e2850f`，并部署本机便携构建 `0.1.0+20260829T034853Z`：真实任务中的21个本地文件链接被安全登记；桌面文本弹窗、390px手机全屏文本与图片预览、不支持格式说明和下载入口、无横向溢出、关闭后阅读位置保持均通过。最终包重启后再次配对读回成功；两次验收临时设备均已删除，设备集合恢复为唯一原设备。设备文件会因原设备正常访问续期而继续变化，不把易变SHA-256当作最终身份依据。
- 历史正文保护：手机页面不再显示启动时注入的插件清单、AGENTS 指令和环境上下文，只显示真实用户消息。
- 启动配置：重复运行启动脚本后，`HOST`、`PORT`、`LAN_NAME` 三项受管配置各保留一份；旧版 `CODEX_PWA_PASSWORD` 会被移除，扫码设备凭证继续保存在用户数据目录。

## 已确认的下一阶段方向

- `DESKTOP-TRAY-WEB-CONSOLE-001` 的托盘 Owner、五区 Redline 管理台和单一授权二维码已有实现候选；旧“内部自动切换”候选已被新决定覆盖。新目标是一次扫码后生成“局域网”和“远程”两个桌面快捷入口，目前尚未实施。
- 管理台使用 faizleecom `time-ai-arms-race-2023@0.1.0` 的 Redline 视觉合同做界面化适配，不把文章组件或第三方品牌资产直接复制进软件。
- 用户只扫码一次绑定手机与电脑；配对成功页分别提供“OpenCodex Link · 局域网”和“OpenCodex Link · 远程”两个快捷入口。两个来源必须映射到同一受信任设备，不再要求密码、重新扫码或新增设备。技术地址仍只在连接详情中显示，Tailscale 不参加物理局域网 mDNS 广播。
- 下一步需在获得明确实施授权后完成双快捷入口，再验证真实手机、NotifyIcon、未知占用和目标机便携包；在此之前不把状态提升为 `delivery_integrated`，也不做 force、PR 或 release。

## 仍需真实手机复验

- 目标手机与当前 Wi-Fi 对 `.local` 的实际解析；失败时应继续停留在扫码得到的 IP 入口。
- 同一次绑定能分别建立局域网和 Tailscale 来源的有效会话，两个来源保持同一设备身份且设备数不增加。
- 浏览器要求用户确认后，两个桌面快捷图标都能正确添加并再次进入。
- 手机选择多附件、原生图片队列失败时的本地路径回退，以及电脑任务中的实际可读结果。

上述事项完成前，不把本轮状态提升为 `delivery_integrated`，也不宣称已经发布或具备公网访问能力。
