{
  "schemaVersion": "1",
  "featureId": "LAN-STABLE-ADDRESS-001",
  "stage": "decision_converged",
  "decisionAuthority": {
    "status": "user_confirmed",
    "evidence": ["docs/product-decisions.json#LAN-STABLE-ADDRESS-001", "docs/product-decisions.json#PHONE-ONE-SCAN-001"]
  },
  "consumers": [
    {
      "class": "product_authority",
      "status": "synced",
      "paths": ["README.md", "docs/desktop-console-design.md"],
      "evidence": "One QR binds the phone to the computer; the post-pairing flow exposes two named shortcuts, local and remote, while keeping raw addresses hidden."
    },
    {
      "class": "decision_ledger",
      "status": "synced",
      "paths": ["docs/product-decisions.json"],
      "evidence": "The structured ledger records a local shortcut backed by LAN or stable name, a remote shortcut backed by Tailscale, diagnostic-only technical addresses, and one shared device identity."
    },
    {
      "class": "conflict_audit",
      "status": "conflict",
      "paths": ["docs/desktop-console-implementation-contract.md", "src/App.tsx", "src/route-failover.ts", "src/console/ConsoleApp.tsx"],
      "evidence": "The current runtime still treats LAN, stable name, and Tailscale as automatic failover candidates and has no local/remote shortcut setup UI. The old behavior is retained only as superseded candidate evidence."
    },
    {
      "class": "downstream_impact",
      "status": "pending",
      "paths": ["docs/desktop-console-implementation-contract.md", "src/console/ConsoleApp.tsx", "server/network.ts", "server/http-app.ts", "server/session.ts"],
      "evidence": "Runtime work must generate both named shortcuts from one binding, establish independent origin sessions for one device row, keep Tailscale outside mDNS, and provide a clear continuation when the remote route is unavailable during initial setup."
    }
  ],
  "checks": [
    {
      "name": "Structured decision JSON parse",
      "status": "passed",
      "command": "Get-Content docs/product-decisions.json -Raw | ConvertFrom-Json",
      "evidence": "The updated structured decision file parses successfully."
    },
    {
      "name": "Current authority and runtime residual scan",
      "status": "pending",
      "command": "rg semantic transport-selection wording in current product and contract sources",
      "evidence": "Authority documents now define the two shortcuts, but runtime source still contains the superseded automatic path-selection behavior and lacks the two shortcut actions."
    }
  ],
  "residualScan": {
    "required": true,
    "status": "pending",
    "scope": ["docs/product-decisions.json", "README.md", "docs/desktop-console-design.md", "docs/desktop-console-implementation-contract.md", "docs/README.md", "src", "server"],
    "patterns": ["Tailscale独立授权二维码", "切换入口新增设备", "网络变化要求重新扫码", "Tailscale加入mDNS广播", "自动路径切换仍作为当前产品体验", "缺少局域网和远程快捷入口"],
    "unresolved": ["Current runtime keeps automatic route probing and relay", "Current management and pairing UI does not create the two named shortcuts"]
  },
  "gate": {
    "allowed": false,
    "blockers": ["The confirmed local and remote shortcut behavior is not implemented", "The new runtime change has not received explicit implementation authorization"],
    "allowedNextAction": "wait_for_explicit_implementation_authorization_then_implement_local_and_remote_shortcuts",
    "stopLine": "Do not modify runtime code without explicit implementation authorization; do not expose the service publicly, add Tailscale to mDNS, or claim the new shortcut flow is delivered."
  }
}
