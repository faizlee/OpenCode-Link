{
  "schemaVersion": "1",
  "featureId": "LAN-STABLE-ADDRESS-001",
  "stage": "decision_converged",
  "decisionAuthority": {
    "status": "user_confirmed",
    "evidence": ["docs/product-decisions.json#LAN-STABLE-ADDRESS-001", "docs/product-decisions.json#PHONE-ONE-SCAN-001"]
  },
  "consumers": [
    {
      "class": "product_authority",
      "status": "synced",
      "paths": ["README.md", "docs/desktop-console-design.md"],
      "evidence": "LAN IP, stable name, Tailscale, and future transports are internal candidates behind one phone-to-computer binding and one QR."
    },
    {
      "class": "decision_ledger",
      "status": "synced",
      "paths": ["docs/product-decisions.json"],
      "evidence": "The structured ledger records automatic reachability-based selection, invisible path switching, diagnostic-only technical addresses, and same-device identity preservation."
    },
    {
      "class": "conflict_audit",
      "status": "synced",
      "paths": ["docs/desktop-console-implementation-contract.md", "src/console/ConsoleApp.tsx"],
      "evidence": "The existing LAN/stable-name/Tailscale buttons are a known superseded implementation candidate, not a competing authority. Tailscale remains excluded from physical mDNS and public-internet claims."
    },
    {
      "class": "downstream_impact",
      "status": "synced",
      "paths": ["docs/desktop-console-implementation-contract.md", "src/console/ConsoleApp.tsx", "server/network.ts", "server/http-app.ts", "server/session.ts"],
      "evidence": "Implementation must add an internal transport selector and failover policy, keep one trusted-device identity across origins, show only one pairing QR, retain technical status for diagnosis, and test fallback without duplicate devices."
    }
  ],
  "checks": [
    {
      "name": "Structured decision JSON parse",
      "status": "passed",
      "command": "Get-Content docs/product-decisions.json -Raw | ConvertFrom-Json",
      "evidence": "The updated structured decision file parses successfully."
    },
    {
      "name": "Current authority residual scan",
      "status": "passed",
      "command": "rg semantic transport-selection wording in current product and contract sources",
      "evidence": "Current authorities expose one QR and make transport selection internal; the source UI mismatch is recorded as an implementation delta."
    }
  ],
  "residualScan": {
    "required": true,
    "status": "passed",
    "scope": ["docs/product-decisions.json", "README.md", "docs/desktop-console-design.md", "docs/desktop-console-implementation-contract.md", "docs/README.md"],
    "patterns": ["Tailscale独立用户二维码", "用户选择连接入口", "切换入口新增设备", "网络变化要求重新扫码", "Tailscale加入mDNS广播"],
    "unresolved": []
  },
  "gate": {
    "allowed": true,
    "blockers": [],
    "allowedNextAction": "separately_authorize_internal_transport_selection_implementation",
    "stopLine": "Do not expose the service publicly, add Tailscale to physical mDNS, alter trusted-device semantics, or implement the new selector without separate authorization."
  }
}
