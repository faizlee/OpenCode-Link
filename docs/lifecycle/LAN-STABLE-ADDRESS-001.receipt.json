{
  "schemaVersion": "1",
  "receiptType": "feature_lifecycle_validation_receipt",
  "featureId": "LAN-STABLE-ADDRESS-001",
  "stage": "decision_converged",
  "manifestDigest": "6a2b9f4f2f52c6a2dc9f2a3154c5bbbac7917c7bff45abf620efd0fd2b521301",
  "allowed": false,
  "blockers": [
    "The confirmed local and remote shortcut behavior is not implemented",
    "The new runtime change has not received explicit implementation authorization",
    "consumer:conflict_audit:conflict",
    "consumer:downstream_impact:pending",
    "check:Current authority and runtime residual scan:pending",
    "residualScan:unresolved",
    "residualScan:pending"
  ],
  "allowedNextAction": "wait_for_explicit_implementation_authorization_then_implement_local_and_remote_shortcuts",
  "stopLine": "Do not modify runtime code without explicit implementation authorization; do not expose the service publicly, add Tailscale to mDNS, or claim the new shortcut flow is delivered.",
  "decisionAuthorityStatus": "user_confirmed",
  "evidenceRefs": [
    "docs/product-decisions.json#LAN-STABLE-ADDRESS-001",
    "docs/product-decisions.json#PHONE-ONE-SCAN-001",
    "README.md",
    "docs/desktop-console-design.md",
    "docs/product-decisions.json",
    "docs/desktop-console-implementation-contract.md",
    "src/App.tsx",
    "src/route-failover.ts",
    "src/console/ConsoleApp.tsx",
    "server/network.ts",
    "server/http-app.ts",
    "server/session.ts",
    "One QR binds the phone to the computer; the post-pairing flow exposes two named shortcuts, local and remote, while keeping raw addresses hidden.",
    "The structured ledger records a local shortcut backed by LAN or stable name, a remote shortcut backed by Tailscale, diagnostic-only technical addresses, and one shared device identity.",
    "The current runtime still treats LAN, stable name, and Tailscale as automatic failover candidates and has no local/remote shortcut setup UI. The old behavior is retained only as superseded candidate evidence.",
    "Runtime work must generate both named shortcuts from one binding, establish independent origin sessions for one device row, keep Tailscale outside mDNS, and provide a clear continuation when the remote route is unavailable during initial setup.",
    "The updated structured decision file parses successfully.",
    "Authority documents now define the two shortcuts, but runtime source still contains the superseded automatic path-selection behavior and lacks the two shortcut actions."
  ],
  "invalidationConditions": [
    "manifest_digest_changed",
    "decision_authority_changed",
    "required_consumer_changed",
    "check_or_residual_scan_changed"
  ]
}
