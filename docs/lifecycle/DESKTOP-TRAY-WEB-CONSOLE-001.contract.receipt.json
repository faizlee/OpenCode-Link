{
  "schemaVersion": "1",
  "receiptType": "feature_lifecycle_validation_receipt",
  "featureId": "DESKTOP-TRAY-WEB-CONSOLE-001",
  "stage": "contract_ready",
  "manifestDigest": "2b0b0980a01c36324ed3ce97ee314cdcce4f0fb777d789c1c43c71954cd94613",
  "allowed": true,
  "blockers": [],
  "allowedNextAction": "complete_user_visible_notifyicon_real_phone_model_rescan_unknown_occupant_and_target_machine_acceptance_then_evaluate_delivery_integrated",
  "stopLine": "Stop on unprovable process identity, guessed phone model, model-based credential merging, user-rename overwrite, device-credential schema drift, new heavyweight runtime need, product semantics conflict, force push, release, PR, remote deletion, or external publication. Do not write delivery_integrated before user-visible NotifyIcon/menu, real-phone model/rescan, unknown-occupant manual comparison, and target-machine acceptance.",
  "decisionAuthorityStatus": "user_confirmed",
  "evidenceRefs": [
    "docs/product-decisions.json#DESKTOP-TRAY-WEB-CONSOLE-001",
    "docs/desktop-console-design.md",
    "docs/desktop-console-implementation-contract.md",
    "README.md",
    "docs/product-decisions.json",
    "scripts/launch.ps1",
    "scripts/stop.ps1",
    "scripts/tray.ps1",
    "src/App.tsx",
    "src/console",
    "src/navigation.ts",
    "server/http-app.ts",
    "server/runtime.ts",
    "server/session.ts",
    "scripts/package-windows.ps1",
    "package.json",
    "src/console/ConsoleApp.tsx",
    "scripts/OpenCodexLink.Identity.psm1",
    "scripts/OpenCodexLink.Service.psm1",
    ".env.example",
    "scripts/OpenCodexLink.Tests.ps1",
    "scripts/OpenCodexLink.Package.Tests.ps1",
    "scripts/OpenCodexLink.TrayIpc.Tests.ps1",
    "docs/README.md",
    "Current product authority records the tray-owned service lifecycle, browser-hosted Redline console, QR-only phone authorization, separate Tailscale direct entry, and implementation-candidate boundary. User-visible NotifyIcon/menu, real-phone rescan and Tailscale migration, unknown-occupant manual comparison, and target-machine acceptance remain pending.",
    "DESKTOP-TRAY-WEB-CONSOLE-001 remains user_confirmed; its 2026-08-28 update removes password login and adds an explicit Tailscale QR/connection entry while automatic download, WebView2, and full native UI remain deferred.",
    "Command scripts are wake or emergency entry points; the tray is the service-lifecycle Owner. Password removal does not open access because every phone request still requires a registered device token. Tailscale remains outside physical mDNS advertising.",
    "Web console, QR-only session APIs, Tailscale address/QR selection, same-device preferred-origin adoption, runtime identity APIs, session rename/batch, tray Owner, packaging, and verification consumers are implemented in this branch as a candidate, not as delivery_integrated.",
    "The chain is launcher to single tray Owner to identified Node service to browser console, then QR-only phone authorization with persistent LOCALAPPDATA credentials and optional same-identity Tailscale adoption.",
    "STA NotifyIcon tray, occupant classification, loopback runtime/shutdown APIs, five-area Redline console, LAN/Tailscale pairing choices, portable-package identity, and main-thread tray IPC are present in source; human NotifyIcon and real-phone Tailscale behavior are not claimed.",
    "The implementation uses Windows PowerShell STA and WinForms already present on target Windows plus the bundled Node runtime; Electron, WebView2, online update, and new external runtimes are excluded.",
    "Tray owns service lifecycle; runtime/tray receipts and process proof gate replacement; Unknown/Unproven occupants are fail-closed; QR is the only authorization owner and trusted-device schema/location remain unchanged.",
    "Version/build identity comes from packaged metadata; runtime and trusted-device records live in LOCALAPPDATA; install .env retains Host/Port/LAN name but removes legacy password configuration; Redline tokens are project-owned CSS.",
    "Automated checks cover QR-only authorization, denial without a device token, disabled password POST, repeated-scan identity reuse, stable/Tailscale origin adoption, Tailscale interface classification, model-first naming, device editing, routing, style scope, build, package smoke, and Headless tray IPC. User-visible NotifyIcon/menu, real-phone model/rescan/Tailscale migration, unknown-occupant comparison, and target-machine acceptance remain.",
    "The next work is human confirmation of the visible NotifyIcon/menu, real-phone QR-only reopen and Tailscale identity migration, unknown-occupant manual comparison, and target-machine portable unzip. Public internet, automatic download, WebView2, full native UI, release, and delivery_integrated remain deferred.",
    "Implementation candidate is on branch codex/desktop-tray-redline in E:/work/project/codexPWA using Cursor CLI, without a Task Control shell, force push, PR, or release.",
    "tsc server and web projects completed with no errors.",
    "Vitest: 16 files, 47 tests passed, including QR-only authorization, rejection without a device credential, disabled password login, Tailscale address classification, phone-model behavior, repeated-scan identity reuse, and cross-origin adoption preserving the device ID and row count.",
    "Server compile and Vite production build completed, including PWA generateSW.",
    "Syntax, package-plan, zip-entry classification, BOM-free JSON interoperability, isolation, Current/Predecessor/StalePid/Unknown, fail-closed unknown-occupant, and isolated Headless tray IPC (ping/status/open/stop/start, same-version wake, shutdown-for-replace mutex release) passed without touching live trusted-device data or stopping the live 8787 service during the isolated suite.",
    "Created release/OpenCodexLink-Windows.zip with dist, dist-server, production node_modules, tray scripts/modules/icon, cmd entries, and build identity; zip checker forbids package-root src/server/.env/trusted-devices.",
    "Zip and extracted root validated; node dist-server started on a random non-8787 port with temp CODEX_PWA_DATA_DIR and HOST=127.0.0.1; /api/health, /api/runtime (no controlToken), and /setup passed; live LOCALAPPDATA unused; tray not started; child PID killed in finally.",
    "The candidate runs from release/deployed-qr-tailscale-20260828-193855/OpenCodexLink with tray PID 62636 and service PID 73104 on build 0.1.0+20260828T113642Z. /api/session reports pairingOnly=true and unauthenticated without a device cookie; old-password POST returns 403; the phone root shows only scan instructions. The management console renders LAN, fixed-name, and Tailscale QR choices and shows http://100.83.218.96:8787 on the connection page. At 390px all five pairing controls remain visible with no horizontal overflow. Tailscale IPv4 and MagicDNS health both pass, same-version launch preserves both PIDs, legacy password is absent from the deployed .env, and trusted-devices.json remains two rows with SHA-256 85436A7D983FF6B126E314FB6279C2AB3278EF9921C3495D3F7323DB0BF6C1F1.",
    "No whitespace errors in the implementation-candidate diff."
  ],
  "invalidationConditions": [
    "manifest_digest_changed",
    "decision_authority_changed",
    "required_consumer_changed",
    "check_or_residual_scan_changed"
  ]
}
