{
  "schemaVersion": "1",
  "receiptType": "feature_lifecycle_validation_receipt",
  "featureId": "DESKTOP-TRAY-WEB-CONSOLE-001",
  "stage": "contract_ready",
  "manifestDigest": "dd3d9f63d1978bd2871a08f8caacc7cadcdaf38792b8eecccf241cdc507796bf",
  "allowed": false,
  "blockers": [
    "Latest dual-shortcut product contract is not implemented or verified",
    "Explicit authorization to modify runtime code for this change is still required",
    "consumer:conflict_audit:conflict",
    "consumer:downstream_impact:pending",
    "consumer:feature_node_map:pending",
    "consumer:runtime_capability_map:pending",
    "consumer:verification_contract:pending",
    "consumer:roadmap:pending",
    "residualScan:unresolved",
    "residualScan:pending"
  ],
  "allowedNextAction": "wait_for_explicit_implementation_authorization_then_implement_test_package_and_verify_dual_shortcut_flow",
  "stopLine": "Do not modify runtime code until explicitly authorized. Do not write delivery_integrated before both shortcuts reopen one real-phone device identity and the remaining tray and target-machine acceptance is verified.",
  "decisionAuthorityStatus": "user_confirmed",
  "evidenceRefs": [
    "docs/product-decisions.json#DESKTOP-TRAY-WEB-CONSOLE-001",
    "docs/desktop-console-design.md",
    "docs/desktop-console-implementation-contract.md",
    "README.md",
    "docs/product-decisions.json",
    "scripts/launch.ps1",
    "scripts/stop.ps1",
    "scripts/tray.ps1",
    "src/App.tsx",
    "src/console",
    "src/navigation.ts",
    "server/http-app.ts",
    "server/runtime.ts",
    "server/session.ts",
    "scripts/package-windows.ps1",
    "package.json",
    "src/console/ConsoleApp.tsx",
    "scripts/OpenCodexLink.Identity.psm1",
    "scripts/OpenCodexLink.Service.psm1",
    ".env.example",
    "scripts/OpenCodexLink.Tests.ps1",
    "scripts/OpenCodexLink.Package.Tests.ps1",
    "scripts/OpenCodexLink.TrayIpc.Tests.ps1",
    "docs/README.md",
    "Current product authority records the tray-owned lifecycle, browser Redline console, one authorization QR, and two named post-pairing shortcuts for local and remote use. The shortcut change is contract-ready but not implemented.",
    "DESKTOP-TRAY-WEB-CONSOLE-001 remains user_confirmed; the 2026-08-29 update keeps one QR for authorization and adds local and remote home-screen shortcuts derived from the same device binding. Automatic download, WebView2, and full native UI remain deferred.",
    "Tray ownership, QR-only authorization, and mDNS boundaries remain aligned. Runtime phone source conflicts with the latest UX because it still performs automatic route relay and does not expose the two shortcut setup actions.",
    "Tray, console, one-QR API, device identity, packaging, and task features exist. The pairing-success UI, local and remote shortcut creation, same-device dual-origin completion, and real-phone acceptance remain pending; the old automatic relay is not delivery evidence for the new contract.",
    "The target chain is launcher to tray Owner to identified Node service to browser console, then one QR binding one phone identity and a pairing-success flow that creates local and remote shortcuts for that identity. Runtime completion is pending.",
    "STA tray, occupant classification, runtime APIs, five-area Redline console, address discovery, one pairing QR, package identity, and tray IPC exist. Dual-shortcut capability and completion state are not present in the runtime.",
    "The implementation uses Windows PowerShell STA and WinForms already present on target Windows plus the bundled Node runtime; Electron, WebView2, online update, and new external runtimes are excluded.",
    "Tray owns service lifecycle; replacement remains fail-closed; QR remains the only authorization owner. Both shortcut origins must resolve to the existing trusted-device identity without destructive schema or data changes.",
    "Version/build identity comes from packaged metadata; runtime and trusted-device records live in LOCALAPPDATA; install .env retains Host/Port/LAN name but removes legacy password configuration; Redline tokens are project-owned CSS.",
    "Existing checks cover QR-only authorization, one visible QR, old migration tickets, identity reuse, Tailscale classification, device management, routing, logs, and rollout behavior. New tests for creating and reopening both named shortcuts as one device are missing.",
    "The new roadmap keeps one authorization QR but replaces invisible automatic switching with two named shortcuts. Implementation authorization, shortcut UI/runtime work, real-phone dual-origin proof, NotifyIcon/menu, unknown-occupant comparison, and target-machine acceptance remain open.",
    "Implementation candidate is on branch codex/desktop-tray-redline in E:/work/project/codexPWA using Cursor CLI, without a Task Control shell, force push, PR, or release.",
    "tsc server and web projects completed with no errors.",
    "Historical candidate evidence: Vitest passed for one-QR authorization, automatic transport fallback, migration tickets, identity reuse, and existing product behavior. These tests predate and do not prove the two-shortcut contract.",
    "Server compile and Vite production build completed, including PWA generateSW.",
    "Syntax, package-plan, zip-entry classification, BOM-free JSON interoperability, isolation, Current/Predecessor/StalePid/Unknown, fail-closed unknown-occupant, and isolated Headless tray IPC (ping/status/open/stop/start, same-version wake, shutdown-for-replace mutex release) passed without touching live trusted-device data or stopping the live 8787 service during the isolated suite.",
    "Created release/OpenCodexLink-Windows.zip with dist, dist-server, production node_modules, tray scripts/modules/icon, cmd entries, and build identity; zip checker forbids package-root src/server/.env/trusted-devices.",
    "Zip and extracted root validated; node dist-server started on a random non-8787 port with temp CODEX_PWA_DATA_DIR and HOST=127.0.0.1; /api/health, /api/runtime (no controlToken), and /setup passed; live LOCALAPPDATA unused; tray not started; child PID killed in finally.",
    "Historical candidate evidence: the deployed old build proved one QR and same-device automatic migration in a desktop browser while preserving live trusted-device data. It did not prove two named home-screen shortcuts on a real phone.",
    "No whitespace errors in the implementation-candidate diff."
  ],
  "invalidationConditions": [
    "manifest_digest_changed",
    "decision_authority_changed",
    "required_consumer_changed",
    "check_or_residual_scan_changed"
  ]
}
